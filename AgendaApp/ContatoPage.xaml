<Page
    x:Class="AgendaApp.ContatoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AgendaApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Novo Contato" FontSize="24px" />
        <StackPanel Grid.Row="1">
            <TextBlock Text="Nome" />
            <TextBox Name="txtNome" />
            <TextBlock Text="Fone" />
            <TextBox Name="txtFone" />
        </StackPanel>
        <CommandBar Grid.Row="2">
            <AppBarButton Icon="Save" Click="btnSalvar_Click"/>
            <AppBarButton Icon="Cancel" Click="btnCancelar_Click"/>
        </CommandBar>
    </Grid>
</Page>
